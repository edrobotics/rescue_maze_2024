#pragma once
#include <mutex>

// Mutexes for reading and writing withing fusion threads
// extern std::mutex mtx_transData_freqData;
// extern std::mutex mtx_transData_controlData;
// extern std::mutex mtx_transData_infreqData;